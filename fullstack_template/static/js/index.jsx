import React from "react";
import ReactDOM from "react-dom";
import App from "./App";

// Load a React class we have created in a separate App.jsx file
ReactDOM.render(<App />, document.getElementById("content"));