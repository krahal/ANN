import React from "react";

// React classes need to be exported to be importable in a different React file
export default class App extends React.Component {
    render () {
        return <p>Hello React!</p>;
    }
}